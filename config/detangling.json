{
  "version": 1,
  "subsystems": [
    {
      "id": "audio",
      "state": "detangled",
      "patterns": ["src/audio/*.c"],
      "documentation": "docs/subsystems/audio.md",
      "next_action": "Expand only when adjacent driver code is recovered."
    },
    {
      "id": "process",
      "state": "detangled",
      "patterns": ["src/process/*.c"],
      "documentation": "docs/subsystems/process.md",
      "next_action": "Revisit address-named subclass destructors when their owning classes are identified."
    },
    {
      "id": "runtime",
      "state": "detangled",
      "patterns": ["src/runtime/*.c"],
      "documentation": "docs/subsystems/runtime.md",
      "next_action": "Trace the relocated routine format and callback-table owners before assigning narrower names."
    },
    {
      "id": "memory",
      "state": "deferred",
      "patterns": ["src/memory/*.c"],
      "documentation": "docs/subsystems/memory.md",
      "next_action": "Determine why the ROM exposes duplicate allocation and free entry points."
    },
    {
      "id": "link",
      "state": "detangled",
      "patterns": ["src/link/*.c"],
      "documentation": "docs/subsystems/link.md",
      "next_action": "Expand when the adjacent multiplayer protocol and connection state are recovered."
    },
    {
      "id": "screens",
      "state": "detangled",
      "patterns": [
        "src/screens/*.c"
      ],
      "documentation": "docs/subsystems/screens.md",
      "next_action": "Expand when the remaining options, backup-clear, and Mario Bros return-path assembly is converted to C."
    },
    {
      "id": "script_engine",
      "state": "detangled",
      "patterns": ["src/script_*.c", "src/script/*.c"],
      "documentation": "docs/subsystems/script-engine.md",
      "next_action": "Expand when adjacent script assembly or command data tables are recovered."
    },
    {
      "id": "mario_bros",
      "state": "active",
      "patterns": ["src/mariobros_*.c"],
      "documentation": "docs/subsystems/mario-bros.md",
      "next_action": "Partition command decoding, state transitions, and global dispatch."
    },
    {
      "id": "field_runtime",
      "state": "queued",
      "patterns": ["src/field_*.c", "src/actor_*.c", "src/activation_*.c"],
      "documentation": "docs/subsystems/field-runtime.md",
      "next_action": "Unify the actor overlay and identify state and command fields from callers."
    },
    {
      "id": "object_runtime",
      "state": "detangled",
      "patterns": [
        "src/object/*.c",
        "src/object_*.c",
        "src/render_object_*.c",
        "src/countdown_*.c",
        "src/*jump*.c",
        "src/*landing*.c"
      ],
      "documentation": "docs/subsystems/object-runtime.md",
      "next_action": "Expand when entry dispatchers or neighboring assembly provide stable gameplay sequence identities."
    },
    {
      "id": "unclassified",
      "state": "queued",
      "patterns": ["src/*.c", "src/*/*.c", "src/*/*/*.c"],
      "documentation": "docs/subsystems/unclassified.md",
      "next_action": "Extract the smallest caller-connected cluster into a real subsystem; do not clean these files wholesale."
    }
  ]
}
